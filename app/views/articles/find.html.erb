<div class="border-bottom pb-4">
  <h1><%= @article.title %></h1>
  <div class="d-flex">
  <%= link_to "", "/articles/edit/#{@article.id}", class:"icon edit" %>
  <%= link_to "", "/articles/#{@article.id}", data: { turbo_method: :delete,turbo_confirm: "Are your sure?" }, class:"icon delete" %>
  </div>
</div>
<div class="mt-2">
  <p><%= @article.content %></p>
</div>

<%= link_to "Back", articles_path, class:"btn btn-primary" %>
